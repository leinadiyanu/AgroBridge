// import { Router } from 'express';
// import { OrdersController } from './controller';

// export function ordersRoutes() {
//   const router = Router();
//   const controller = new OrdersController(null as any);

//   router.post('/order', (req, res) => controller.createOrder(req, res));
//   router.get('/order/:id', (req, res) => controller.getOrder(req, res));

//   return router;
// }
